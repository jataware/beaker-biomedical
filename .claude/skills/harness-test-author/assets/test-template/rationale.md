# Intended Behavior

<The correct method, in plain prose, and the live-verified ground-truth result (with the value and
the date you verified it). This is design rationale only — it is NEVER graded. Every claim here that
should affect the score must also appear as a check in eval.yaml.>

# Incorrect Behavior

<The specific trap a weak agent falls into: the plausible wrong number, the hallucinated capability,
the wrong field/table/denominator. Each trap should map to a check (usually must_not_contain, or the
absence of a behavior).>
