---
name: "The bracketed-string array quirk on `*Overview` rows"
description: "This tests `participantOverview` row retrieval with paging past the default `first: 10`, and the CTDC quirk that multi-valued `*Overview` fields come back as bracketed strings, not JSON arrays."
---
List the Colorectal Carcinoma participants with their targeted therapies as a clean list.
