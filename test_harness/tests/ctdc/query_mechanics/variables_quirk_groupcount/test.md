---
name: "First call: the `variables` quirk, totals, and `GroupCount.subjects`"
description: "This tests the three first-call rules at once — POST-only, the required `variables` key, and the faceted count field being `subjects` not `count` — using an unfiltered `searchParticipants` as the totals call."
---
How many participants are in CTDC, and what's the race breakdown?
