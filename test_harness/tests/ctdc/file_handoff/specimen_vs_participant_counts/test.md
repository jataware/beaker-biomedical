---
name: "Specimen/file counts are NOT participant counts; files download via DRS"
description: "This tests two structural facts: specimen/file facet counts are per-specimen/per-file (not per-participant) and can exceed the participant total, and the API never streams bytes — files carry a CRDC DRS id with no signed URL."
---
How many specimens of a given type are in CMB, and how do I download the data files?
