checks:
  - number:
      name: "mzml_files"
      target: 46792
      tolerance_percent: 15
  - behavior: "counted the files with a file query on format = mzML (e.g. summarize_files(match_all=['format = mzML']))"
  - behavior: "routed the quantitation analysis to the proteomic-data-commons"
  - behavior: "noted CDA returns drs_uri only and holds no abundance values"
