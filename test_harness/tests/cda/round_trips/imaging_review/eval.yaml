checks:
  - behavior: "located the glioblastoma cohort's DICOM files in CDA via a file query on format = DICOM (e.g. summarize_files or get_file_data), the files that carry drs_uri"
  - behavior: "routed series and viewing to the imaging-data-commons rather than expecting CDA to render or serve the image pixels"
