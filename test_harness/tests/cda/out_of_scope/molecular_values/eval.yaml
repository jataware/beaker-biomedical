checks:
  - behavior: "stated that CDA holds no protein-abundance/expression values (CDA is metadata-only and can only locate the cohort)"
  - behavior: "routed the EGFR protein-abundance request to the proteomic-data-commons"
  - behavior: "did not fabricate or report any numeric EGFR protein-abundance value (correctly declined, since CDA has none)"
