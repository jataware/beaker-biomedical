checks:
  - number:
      name: "cisplatin_subjects"
      target: 1168
      tolerance_percent: 15
  - behavior: "used the therapeutic_agent column (discovered via column_values), not an invented drug/treatment column"
  - behavior: "reported the count of distinct treated subjects (~1168), not the larger value-occurrence / treatment-row count (~1270) — the number tolerance alone cannot separate these"
