checks:
  - number:
      name: "subjects"
      target: 328
      tolerance_percent: 15
  - behavior: "used the range filter 'year_of_birth < 1950' with spaces around the operator"
  - behavior: "filtered species = human"
  - behavior: "filtered for a recorded cause of death using the CDA NULL keyword (e.g. 'cause_of_death != NULL'), not an enumerated value list or a guessed value"
