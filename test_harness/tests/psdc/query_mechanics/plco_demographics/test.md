---
name: "PLCO demographics (`studyDemographics`, the `subjects` subselection)"
description: "This tests the study-level `studyDemographics` query with the required `variables` key, the `{ group subjects }` subselection on `[GroupCounts]`, and the count field being `subjects`, not `count`."
---
Give me the age, sex, and race breakdown of PLCO participants in the Population Sciences Data Commons.
