---
name: "Files → CRDC DRS → manifest, no byte download"
description: "This tests that the agent fetches file records, resolves the CRDC DRS id and access info via `fileDetail`/`fileInfo`, and routes downloading through DRS or a manifest rather than a byte fetch."
---
List the files for the UBC01 study (or for a bladder-cancer case) and tell me exactly how I'd download them.
