---
name: "Open vs controlled access"
description: "This tests the `files.access` field and the open-access model — splitting a project's files into `open` vs `controlled` rather than assuming everything is open or everything needs a token."
---
For the TCGA-PAAD project, how many files can I download without a token, and how many require controlled access?
